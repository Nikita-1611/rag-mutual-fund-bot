# Pure Python Requirements (No compilation, no Rust/C++)
# ---------------------------------------------------

# Web Server (High quality wheels available for all platforms)
fastapi
uvicorn
gunicorn
python-dotenv

# API Clients (Pure Python)
pinecone-client
cohere
langchain_groq
langchain_core
huggingface_hub

# Note: All heavy local model libraries (torch, fastembed, onnx) 
# have been replaced by API calls to ensure no compilation on Render.
