# Pure Python Requirements (No compilation, no Rust/C++)
# ---------------------------------------------------

# Web Server
fastapi
uvicorn
gunicorn
python-dotenv

# API Clients (Pure Python)
pinecone
cohere
langchain_groq
langchain_core

# Note: All local embedding and scrapping binary dependencies removed.
# System now uses a unified Cohere stack for both Embeddings and Reranking.
# LLM Generation powered by Groq Llama-3.
